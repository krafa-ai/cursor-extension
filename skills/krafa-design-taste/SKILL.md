---
name: krafa-design-taste
description: Call the design_taste tool before building or restyling UI so the work matches this account's taste. Use when designing, restyling, or animating an interface.
---

When the request is to design, restyle, or animate an interface, call the `design_taste` MCP tool first and build to what it returns. Do not invent a visual system when this tool is available. The payload is the whole standard; read the routing at the top, then the matching sections.
